# Admin Portal Frontend Environment Variables
# These will be exposed to the browser, so do not put sensitive keys here.

# PocketBase API URL
VITE_POCKETBASE_URL=https://api.cloudfreedom.de

# Billing API URL
VITE_BILLING_API_URL=https://billing.cloudfreedom.de

# Authentication
# The Admin Portal uses PocketBase authentication.
# Users log in with email/password, and PocketBase returns an auth token.
# This token is used for all API calls to the Billing API.
# No API keys are exposed to the frontend - only PocketBase Auth Tokens!
